// eslint-disable-next-line no-unused-vars
import classnames from 'classnames'
import { Nav } from '../semantics/index'
export default function NavBar() {
  return (
    <Nav
      className={`
        
      `}
    >
      
    </Nav>
  )
}